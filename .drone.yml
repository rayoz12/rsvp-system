---
kind: pipeline
name: default

clone:
  disable: true

steps:
- name: test
  image: docker:dind
  volumes:
  - name: dockersock
    path: /var/run/docker.sock
  commands:
    - sleep 5 # give docker enough time to start
    - docker ps -a

volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock